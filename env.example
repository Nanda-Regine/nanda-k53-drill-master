# K53 Drill Master — Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git (it's already in .gitignore)

# OpenAI API key for the AI Tutor (Explain This button)
# Get yours at https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=sk-your-key-here

# Supabase project credentials (magic-link auth for subscribers)
# Find these in your Supabase project → Settings → API
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Supabase server-side keys (used in Vercel serverless functions only — NEVER expose to browser)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Your live site URL (used in magic link redirect after payment)
SITE_URL=https://nanda-k53-drill-master.vercel.app
